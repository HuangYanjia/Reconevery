# Articulation alignment image

CPU image for static-evidence state Sim(3) alignment and analytic measured joint
estimation. Mount only the attempt workspace at `/workspace`; do not mount source
run roots.
