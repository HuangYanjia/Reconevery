seed: 42
stages:
  sam3d_object_candidates:
    adapter:
      name: sam3d_object_candidates
      config:
        execution_mode: fake_worker
        worker_python: python
        worker_script: tests/fixtures/fake_sam3d_objects_worker.py
        fake_mode: success
    depends_on: [completion_evidence_package]
